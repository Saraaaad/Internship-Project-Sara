---
description: Check if a controller's source files have changed since the last UI generation. Reports which files changed and whether UI regeneration is needed.
agent: uica
---

Check if the following controller has changed:

$ARGUMENTS

Load the `detect-backend-changes` skill and report the results.

Do NOT generate any UI code. Only report what changed.
